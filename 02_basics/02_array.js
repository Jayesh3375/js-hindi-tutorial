const Arr = [3, 5, 7, 4, 8, 3, 2];

// console.log(Arr);

// const newArr = Arr.push(3);  // It adds a new element into an array
// console.log(Arr);  // It returns a new modified array.
// console.log(newArr); // .it returns the new length of an array.

// const newArr = Arr.pop(); //removes the last element from an array
// console.log(Arr); // It returns a new modified array.
// console.log(newArr); // .it returns the new length of an array.

const newArr = Arr.unshift(3);
console.log(Arr);